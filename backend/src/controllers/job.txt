const job= require("../models/job");
const createJob = async (req, res, next)=>{
    try{
        const attachments = req.files? req.files.map(e => e.path) : [];
        const newJob = job.create(...req.body, attachements, client: req.user._id)
        res.s
    }catch(err){
        next(err)
    }
}